import 'package:flutter/material.dart';

class project_nav_frm2 extends StatelessWidget {
  const project_nav_frm2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
          body: Column(
            children: [
              Text("Navigation-Form"),
        ]
    )
    );
        }
}
